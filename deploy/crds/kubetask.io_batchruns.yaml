---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: batchruns.kubetask.io
spec:
  group: kubetask.io
  names:
    kind: BatchRun
    listKind: BatchRunList
    plural: batchruns
    singular: batchrun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.batchRef
      name: Batch
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.progress.completed
      name: Completed
      type: integer
    - jsonPath: .status.progress.total
      name: Total
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BatchRun represents a specific execution instance of a Batch.
          Each execution creates a new BatchRun.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of BatchRun
            properties:
              agentTemplateRef:
                description: |-
                  Agent template reference (optional override)
                  If not specified, uses WorkspaceConfig default or convention-based discovery
                properties:
                  key:
                    description: |-
                      Key within the ConfigMap that contains the Job template YAML
                      If not specified, defaults to "agent-template.yaml"
                    type: string
                  name:
                    description: Name of the ConfigMap containing the agent template
                    type: string
                type: object
              batchRef:
                description: Reference to an existing Batch
                type: string
              batchSpec:
                description: |-
                  Inline Batch definition (alternative to BatchRef)
                  Allows defining the batch directly without creating a separate Batch resource
                properties:
                  commonContext:
                    description: |-
                      CommonContext - contexts shared across all tasks (the constant part)
                      These contexts are included in every task
                    items:
                      description: |-
                        Context represents different types of task inputs
                        This is a polymorphic type that can represent File, Repository, API, Database, etc.
                      properties:
                        file:
                          description: File context (required when Type == "File")
                          properties:
                            name:
                              description: File name (e.g., "task.md", "config.json")
                              type: string
                            source:
                              description: File content source (exactly one must be
                                specified)
                              properties:
                                configMapKeyRef:
                                  description: Reference to a key in a ConfigMap
                                  properties:
                                    key:
                                      description: Key of the ConfigMap to select
                                        from
                                      type: string
                                    name:
                                      description: Name of the ConfigMap
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap must
                                        be defined
                                      type: boolean
                                  type: object
                                inline:
                                  description: Inline content
                                  type: string
                                secretKeyRef:
                                  description: Reference to a key in a Secret
                                  properties:
                                    key:
                                      description: Key of the secret to select from
                                      type: string
                                    name:
                                      description: Name of the secret
                                      type: string
                                    optional:
                                      description: Specify whether the Secret must
                                        be defined
                                      type: boolean
                                  type: object
                              type: object
                          type: object
                        repository:
                          description: Repository context (required when Type == "Repository")
                          properties:
                            branch:
                              description: Branch name
                              type: string
                            org:
                              description: GitHub organization name
                              type: string
                            repo:
                              description: Repository name
                              type: string
                          type: object
                        type:
                          description: 'Type of context: File, Repository, API, Database,
                            etc.'
                          enum:
                          - File
                          - Repository
                          type: string
                      type: object
                    type: array
                  variableContexts:
                    description: |-
                      VariableContexts - variable contexts that differ per task (the variable part)
                      Each item is a list of contexts for one task
                      Task[i] = commonContext (constant) + variableContexts[i] (variable)
                      Total tasks = len(variableContexts)


                      Example:
                        commonContext: [task.md, guide.md]  // constant - same for all tasks
                        variableContexts: [                 // variable - different per task
                          [repo1:main, repo1-config.json],  // task 1's variable part
                          [repo2:main, repo2-config.json],  // task 2's variable part
                        ]
                        Generates 2 tasks:
                          Task 1 = [task.md, guide.md] + [repo1:main, repo1-config.json]
                          Task 2 = [task.md, guide.md] + [repo2:main, repo2-config.json]
                    items:
                      description: ContextSet represents a set of contexts for one
                        task
                      items:
                        description: |-
                          Context represents different types of task inputs
                          This is a polymorphic type that can represent File, Repository, API, Database, etc.
                        properties:
                          file:
                            description: File context (required when Type == "File")
                            properties:
                              name:
                                description: File name (e.g., "task.md", "config.json")
                                type: string
                              source:
                                description: File content source (exactly one must
                                  be specified)
                                properties:
                                  configMapKeyRef:
                                    description: Reference to a key in a ConfigMap
                                    properties:
                                      key:
                                        description: Key of the ConfigMap to select
                                          from
                                        type: string
                                      name:
                                        description: Name of the ConfigMap
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          must be defined
                                        type: boolean
                                    type: object
                                  inline:
                                    description: Inline content
                                    type: string
                                  secretKeyRef:
                                    description: Reference to a key in a Secret
                                    properties:
                                      key:
                                        description: Key of the secret to select from
                                        type: string
                                      name:
                                        description: Name of the secret
                                        type: string
                                      optional:
                                        description: Specify whether the Secret must
                                          be defined
                                        type: boolean
                                    type: object
                                type: object
                            type: object
                          repository:
                            description: Repository context (required when Type ==
                              "Repository")
                            properties:
                              branch:
                                description: Branch name
                                type: string
                              org:
                                description: GitHub organization name
                                type: string
                              repo:
                                description: Repository name
                                type: string
                            type: object
                          type:
                            description: 'Type of context: File, Repository, API,
                              Database, etc.'
                            enum:
                            - File
                            - Repository
                            type: string
                        type: object
                      type: array
                    type: array
                type: object
            type: object
          status:
            description: Status represents the current status of the BatchRun
            properties:
              completionTime:
                description: Completion time
                format: date-time
                type: string
              conditions:
                description: Kubernetes standard conditions
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Execution phase
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                type: string
              progress:
                description: Progress statistics
                properties:
                  completed:
                    description: Number of completed tasks
                    type: integer
                  failed:
                    description: Number of failed tasks
                    type: integer
                  pending:
                    description: Number of pending tasks
                    type: integer
                  running:
                    description: Number of running tasks
                    type: integer
                  total:
                    description: Total number of tasks
                    type: integer
                type: object
              startTime:
                description: Start time
                format: date-time
                type: string
              tasks:
                description: Task details list
                items:
                  description: TaskStatus represents the status of a single task
                  properties:
                    completionTime:
                      description: Completion time
                      format: date-time
                      type: string
                    contexts:
                      description: |-
                        Task contexts (commonContext + one target)
                        This is what makes this task unique
                      items:
                        description: |-
                          Context represents different types of task inputs
                          This is a polymorphic type that can represent File, Repository, API, Database, etc.
                        properties:
                          file:
                            description: File context (required when Type == "File")
                            properties:
                              name:
                                description: File name (e.g., "task.md", "config.json")
                                type: string
                              source:
                                description: File content source (exactly one must
                                  be specified)
                                properties:
                                  configMapKeyRef:
                                    description: Reference to a key in a ConfigMap
                                    properties:
                                      key:
                                        description: Key of the ConfigMap to select
                                          from
                                        type: string
                                      name:
                                        description: Name of the ConfigMap
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          must be defined
                                        type: boolean
                                    type: object
                                  inline:
                                    description: Inline content
                                    type: string
                                  secretKeyRef:
                                    description: Reference to a key in a Secret
                                    properties:
                                      key:
                                        description: Key of the secret to select from
                                        type: string
                                      name:
                                        description: Name of the secret
                                        type: string
                                      optional:
                                        description: Specify whether the Secret must
                                          be defined
                                        type: boolean
                                    type: object
                                type: object
                            type: object
                          repository:
                            description: Repository context (required when Type ==
                              "Repository")
                            properties:
                              branch:
                                description: Branch name
                                type: string
                              org:
                                description: GitHub organization name
                                type: string
                              repo:
                                description: Repository name
                                type: string
                            type: object
                          type:
                            description: 'Type of context: File, Repository, API,
                              Database, etc.'
                            enum:
                            - File
                            - Repository
                            type: string
                        type: object
                      type: array
                    jobName:
                      description: Kubernetes Job name
                      type: string
                    startTime:
                      description: Start time
                      format: date-time
                      type: string
                    status:
                      description: Task status
                      enum:
                      - Pending
                      - Running
                      - Succeeded
                      - Failed
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
