---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: workspaceconfigs.kubetask.io
spec:
  group: kubetask.io
  names:
    kind: WorkspaceConfig
    listKind: WorkspaceConfigList
    plural: workspaceconfigs
    singular: workspaceconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          WorkspaceConfig defines the workspace environment for task execution.
          Workspace = AI agent + permissions + tools + infrastructure
          This is the execution black box - Batch creators don't need to understand execution details.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the workspace configuration
            properties:
              agentTemplateRef:
                description: |-
                  Agent template reference
                  Reference to a ConfigMap containing the agent Job template.
                  If not specified, controller uses convention-based discovery (ConfigMap named "kubetask-agent").
                  If convention-based ConfigMap not found, controller uses built-in default template.
                properties:
                  key:
                    description: |-
                      Key within the ConfigMap that contains the Job template YAML
                      If not specified, defaults to "agent-template.yaml"
                    type: string
                  name:
                    description: Name of the ConfigMap containing the agent template
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
